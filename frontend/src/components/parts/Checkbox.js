import ReactCheckbox from 'rc-checkbox';
import 'rc-checkbox/assets/index.css';

import styled from 'styled-components';

const Checkbox = styled(ReactCheckbox)`
  font-size: 0;
`;

export default Checkbox;
